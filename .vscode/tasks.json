{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build wlan_dev (CMake)",
			"type": "shell",
			"command": "cmake -S code_wlan/wlan_mac -B build -G \"Unix Makefiles\" && cmake --build build -j$(nproc)",
			"isBackground": false,
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}